{
  "compilerOptions": {
    // FIX for Module '"faker"' can only be default-imported using the 'esModuleInterop' flag
    "esModuleInterop": true,
    // FIX for "Can not find name googlemaps" after installing
    // @types/googlemaps.
    // "types": ["faker", "googlemaps"]
  },
  "include": [
    "images",
    "src",
  ],
  "exclude": [
    "node_modules"
  ]
}
